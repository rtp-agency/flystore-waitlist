{
  "name": "flystore-waitlist",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "local": "astro build && node --env-file-if-exists=.env scripts/serve.mjs",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "astro": "^5.14.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.0",
    "tailwindcss": "^4.1.0"
  }
}
